# E-commerce

Static storefront landing page deployed with Vercel.

## Local Preview

Open `index.html` in a browser.

## Deploy

This project is a zero-build static site, so Vercel can serve it directly from the repository root.
